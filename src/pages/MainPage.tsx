import React from 'react';
import {Header} from "../components/Header/Header";

export const MainPage = () => {
    return (
        <div>
            MAIN
            <Header />
        </div>
    );
};